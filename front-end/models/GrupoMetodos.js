class GrupoMetodos {
    constructor(x, y, ancho, alto) {
        this.x = x;
        this.y = y;
        this.ancho = ancho;
        this.alto = alto;
        this.metodos = [];
        this._id = -1;
    }
}